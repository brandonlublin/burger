<h1></h1>
<p></p> 
<div class="jumbotron jumbotron-fluid">
    <div class='container'>
    <h1 class="display-4">Eat Tha Burger!</h1>
    <br><br>
    <hr>
    <form>
        <div class="form-group">
            <label for="exampleInputEmail1">Do none of our options sound good? Create your own!</label>
            <input type="data" class="form-control" id="burgerInput" placeholder="Enter New Burger">
        </div>
    </form>
    <a class="btn btn-primary btn-lg" href="#" role="button" id='burgerMe'>Burger Me Bra!</a>
    </div>
</div>
<div class="container">
    <div class='row'>
        <div class='col-md-6'>
            <h4>Menu Of Burgers:</h4>
            <ul>
                {{#each burger}}
                    {{#unless devoured}}

                    {{/unless}}
                {{/each}}
            </ul>
        </div>
        <div class='col-md-6'>
            <h4>Burgers You've Devoured:</h4>
            <ul>
                {{#each burger}}
                    {{#if devoured}}

                    {{/if}}
                {{/each}}
            </ul>
        </div>
    </div>
</div>